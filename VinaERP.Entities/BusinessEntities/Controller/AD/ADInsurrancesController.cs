using System;
using System.Data;
using System.Text;
using System.Collections.Generic;
using VinaLib;
using System.Collections;

namespace VinaERP
{
    #region ADInsurrances
    //-----------------------------------------------------------
    //Generated By:VinaERP Studio
    //Class:ADInsurrancesController
    //Created Date:Wednesday, December 11, 2019
    //-----------------------------------------------------------

    public class ADInsurrancesController : BaseBusinessController
    {
        public ADInsurrancesController()
        {
            dal = new DALBaseProvider("ADInsurrances", typeof(ADInsurrancesInfo));
        }

        public override IList GetListFromDataSet(DataSet ds)
        {
            List<ADInsurrancesInfo> list = new List<ADInsurrancesInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    ADInsurrancesInfo obj = (ADInsurrancesInfo)GetObjectFromDataRow(row);
                    list.Add(obj);
                }
            }
            return list;
        }
    }
    #endregion
}